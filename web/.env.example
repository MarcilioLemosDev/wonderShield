# URL do motor (servidor WebSocket). Em dev local, o padrao ja aponta para ca.
NEXT_PUBLIC_ARENA_WS=ws://localhost:8765

# Supabase (preencha com o seu projeto)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# chave de servico: so no servidor, nunca exposta ao cliente
SUPABASE_SERVICE_ROLE_KEY=
